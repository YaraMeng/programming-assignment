# Core scientific / plotting stack
numpy==2.3.3          # numeric arrays & stats
matplotlib==3.10.6    # plotting (polar bars, interactivity backend)
requests==2.32.5      # HTTP fetch for Open-Meteo API

# mplcursors removed (no longer used: replaced by custom motion_notify_event hover)
# If you prefer mplcursors-based pinned tooltips, you can optionally install:
# mplcursors==0.7

# (Optional) If you run into font issues on some systems you might also add:
# pillow==10.4.0      # for some image backends
